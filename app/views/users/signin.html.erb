<%= form_for :user do |f| %>
  <%= f.label :name %>
  <%= f.collection_select :name, @users, :name, :name %>

  <%= f.label :password %>
  <%= f.password_field :password, id: 'password' %>

  <%= f.submit 'Sign In' %>
<% end %>
